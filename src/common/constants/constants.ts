// eslint-disable-next-line import/prefer-default-export
export const DesignPatterns = {
  strategy: {
    title: 'STRATEGY PATTERN',
    message: 'The Strategy pattern defines a family of algorithms, encapsulates each one, and makes them interchangeable. Strategy lets the algorithm vary independently from the clients that use it.',
  },
};
